# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 规范，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 修复

- **（P0）自动保存竞态**：在途写入期间切换文档会把旧文档的版本号污染到新文档，导致新文档静默停止保存且界面显示「已保存」。现在待写快照携带文档 ID，会话代际切换后在途结果整体作废
- **（P0）删除与在途保存并发**：已删除文档可能被在途写入"复活"。存储层 `updateDocument` 对不存在的文档不再隐式重建，删除语义优先
- **（P0）初始化失败后「重试」失效**：失败分支误将加载态置为已完成，现可正常重试
- **（P1）关闭窗口丢稿**：`beforeunload` 的异步写入不可靠，新增 `pagehide` / `visibilitychange` 双通道 + localStorage 崩溃缓冲（携带文档 ID），下次启动检测并提示恢复
- **（P1）Windows/Linux 快捷键冲突**：`Ctrl+Shift+T` 被主题切换分支抢占，打字机模式快捷键不可达
- **（P1）删除保护**：保存失败时删除文档将中止，防止未保存内容随删除丢失
- **（P1）图片容量预算**：新增单文档 24MB 图片总量上限与 4000 万像素解码上限，防止粘贴大图导致内存耗尽
- **（P1）切主题后 Mermaid 不重绘**：已渲染图表的配色现在跟随明暗主题
- **（P1）切文档后导出张冠李戴**：导出 HTML 改为始终走净化渲染管线，仅 Mermaid 文档取预览快照且校验归属
- 快速连续切换文档的竞态（请求令牌）、并发删除产生孤儿文档、保存失败 Toast 无限堆积、新建文档标题重名、初始化被 IndexedDB 探测阻塞永久卡加载等

### 安全加固

- Electron 导航守卫改为默认拒绝（仅放行本地同路径），修复 URL 解析异常时守卫被绕过
- 会话层注入 CSP 响应头（不依赖 meta 标签在 `file://` 下的行为）
- IPC 文件导入：20MB 上限 + 二进制嗅探 + 移除「所有文件」过滤器 + 错误信息脱敏（不再回传本地绝对路径）
- 权限请求全量拒绝（`setPermissionRequestHandler` / `setPermissionCheckHandler`）
- DOMPurify：URI 白名单收窄到栅格图；`foreignObject` 移出 Markdown 路径白名单；净化 hook 大小写无关（覆盖 SVG 内 `<a>`）；链接强制 `rel="noopener noreferrer nofollow"`
- KaTeX 显式锁定 `trust: false` 与宏展开上限（`maxExpand` / `maxSize`）
- 导出的独立 HTML 附加 `default-src 'none'` 系 CSP
- `newId()` 降级分支改用 CSPRNG

### 移动端

- 触摸目标全面达标（≥44px）：状态栏开关/重试、Toast 关闭、搜索清除、排序、面板关闭、命令面板入口等 9 处
- 底部安全区修正：状态栏处理 iPhone Home 指示条遮挡（此前打字机开关不可点），格式条错位的 safe-area 移除
- 顶栏长标题不再把功能按钮挤出屏幕（面包屑可收缩 + truncate 生效）
- 底部格式条按钮间距 2px → 4px 并支持横向滚动，降低误触
- 移动端新增「导出」直达入口；抽屉侧栏增加入场动效、顶部安全区与关闭按钮
- 桌面/移动分段控件收敛为共享 `Segmented` 组件，激活态视觉统一
- 触屏 hover 粘滞修复（`@custom-variant hover` 限定 hover-capable 设备）
- 视图状态跨断点保持（旋转屏/拉宽窗口不再丢失编辑/预览选择）；键盘弹起适配（`interactive-widget=resizes-content`）

### 性能

- 状态栏字数统计从「每次击键全文扫描 4 遍」改为单趟游标统计 + 500ms 防抖
- 光标行计算从 O(全文) 降为 O(光标所在行宽)
- Mermaid 增加「主题 + 源码」级缓存，源码未变不重渲染，并以世代号丢弃过期渲染
- **首屏主 bundle 909KB → 409KB（gzip 293 → 133KB，-55%）**：导出与 Markdown 渲染链改为按需异步加载
- KaTeX 由双版本（0.16.47 嵌套 + 0.18.4）统一为单一版本，消除 JS/CSS 版本错位
- 字体自托管（移除 Google Fonts 外链），离线可用、首屏零外部请求
- 光标行高亮改为「单文本节点镜像 + Range 测量」，万行文档每键从 O(总行数) 降为 O(光标行宽)
- 预览渲染改异步加载 + 自适应防抖（300ms~1.2s 按上次耗时），长任务移出输入热路径
- 滚动同步增加回声防护（最近主动滚动方 + 250ms 静默窗 + 2px 死区）
- 格式命令改用 `setRangeText` 写入，保留浏览器原生撤销栈

## [1.2.0] - 2026-08

### 新增

- Electron 38 桌面版（Windows NSIS 安装包 / 便携版），与 Web 版共用核心代码
- 打开本地 `.md` 文件（原生文件对话框，Electron 与 Web 双路径）
- Mermaid 图形渲染：流程图 / 序列图 / 甘特图 / 饼图，明暗主题跟随
- LaTeX 数学公式（KaTeX：`$...$` 行内 / `$$...$$` 块级）
- 命令面板（Cmd/Ctrl+K）、快捷键面板、打字机模式、分屏滚动同步

## [1.1.0] - 2026-07

### 新增

- 移动端响应式布局（单栏 Tab + 抽屉侧栏 + 底部工具栏）
- 明暗双主题（跟随系统 + 持久化 + 挂载前防闪烁）
- 导出 HTML（内联样式自包含）与纯文本
- 多文档管理：新建 / 打开 / 重命名 / 删除 / 搜索过滤

## [1.0.0] - 2026-07

### 新增

- 所见即所得 Markdown 编辑器（编辑 / 预览双栏，300ms 实时渲染）
- 图片粘贴 / 拖拽自动压缩并 base64 内嵌，单文件自包含
- 800ms 防抖自动保存，IndexedDB 本地持久化
- 「块面派」视觉体系（冷白基底 / 焦点蓝强调色 / 设计 Token）
